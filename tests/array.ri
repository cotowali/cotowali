fn echo_arr(arr []int) {
  echo('echo_arr')
  arr
}

fn raw_arr(arr []int) []int {
  return arr
}

[0, 1, 2]
let v = ['a', 'b', 'c']
v

let arr = [0, 1, 2]
echo_arr(arr)
echo_arr([0, 1, 2])
raw_arr(arr)
// assert raw_arr(arr) == arr
