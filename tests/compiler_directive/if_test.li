#if true
  assert(true)
#else
  assert(false)
#endif

#if false
  assert(false)
#else
  assert(true)
#endif

#if !true
  assert(false)
#else
  assert(true)
#endif

#if !!true
  assert(true)
#else
  assert(false)
#endif
