default: hello
tasks:
  hello: z run examples/hello.vash
  run: v run cmd/vash/main.v $@
  build: v cmd/vash/main.v
  test: v test .
  test.hello: z run examples/hello.vash
  fmt: v fmt -w .
  ast: z run tools ast $@
  tokens: z run tools tokens $@
